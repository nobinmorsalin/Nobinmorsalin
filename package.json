{
  "name": "nobin-portfolio",
  "version": "1.0.0",
  "description": "Nobin's professional portfolio with admin panel",
  "private": true,
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'Static site — no build step needed'",
    "start": "vercel dev"
  },
  "dependencies": {
    "nodemailer": "^6.9.14",
    "@neondatabase/serverless": "^1.0.1"
  },
  "engines": {
    "node": "24.x"
  }
}
